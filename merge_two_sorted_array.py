# Merge two arrays and sort them

arr1 = [1, 3, 5, 2]
arr2 = [2, 4, 6]

# Combine both arrays
merged_array = arr1 + arr2

# Sort the combined array
merged_array.sort()  # same as Collections.sort()

print(f"Merged and sorted array : {merged_array}")